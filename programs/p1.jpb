func master(): None {
    mut a: int
    mut b: int
    mut c: int
    
    read(a)
    read(b)
    read(c)

    if(a > b and a > c) {
        write(a)
    } else if(b > a and b > c) {
        write(b)
    } else {
        write(c)
    }
}
    
