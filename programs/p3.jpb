func master() {
    mut n: int
    read(n)
    
    elems: List<int>

    while(n > 1) {
        elems.insert(n)
        if(n % 2 == 1) {
            n = n*3 + 1
        }
        else {
            n = n/2
        }
    }
    for(elem: elems) {
        write(elem)
    }
}
